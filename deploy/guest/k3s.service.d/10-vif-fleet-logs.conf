[Unit]
# Pull in the path's mount unit and order K3s after it. If the mount cannot
# start, K3s cannot start and session logs never fall through to the root disk.
RequiresMountsFor=/var/log/vif-fleet
