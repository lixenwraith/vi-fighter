#compdef vif
# Generated from cmd/vif/usage.go by TestGeneratedFilesAreTheHelpTable; do not edit.
_arguments \
  '-host[Play at once and open the game to participants, e.g. :7777]:addr: ' \
  '-serve[Bind a headless session with no local cursor]:addr: ' \
  '-join[Join a session at \[tcp://|vif://\]host:port\[/name\] or a wss:// route; tcp when no scheme is given]:addr: ' \
  '-name[Name this host answers to, so one address can serve several sessions]:name: ' \
  '-players[Roster ceiling including self, 2..16; unset holds the whole roster]:n: ' \
  '-authority[Where authorship goes when the authoring participant leaves; default host with -serve, migrate otherwise]:value:(host migrate)' \
  '-slow-window[Window a participant'\''s lateness is judged over; host only, 0 never evicts (default 20s)]:dur: ' \
  '-slow-late[Evict at this many late epochs per second over the window; 0 ignores lateness (default 5)]:n: ' \
  '-slow-bytes[...and at least this many bytes per second on its link; 0 ignores throughput (default 8192)]:n: ' \
  '-listen[Address this participant is dialled back on; -join only, default the host'\''s port then an ephemeral one]:addr: ' \
  '-no-advertise[Keep this participant'\''s address out of the session; -join only, and it is never elected]' \
  '-size[Terminal-equivalent size for a run that has no terminal]:WxH: ' \
  '-probe[Health and metrics endpoint; -serve only]:addr: ' \
  '-first-join[Exit if no guest connects within this; -serve only, 0 waits forever]:dur: ' \
  '-empty[Exit this long after the last guest leaves; -serve only, 0 keeps the session]:dur: ' \
  '-drain[How long a termination signal waits for an empty roster; -serve only, 0 exits at once]:dur: ' \
  '(-d -config-embedded)'{-d,-config-embedded}'[Use the embedded scenario and content, ignoring -s and -f]' \
  '-config-dir[Configuration root holding scenario/ input/ audio/ content/ image/]:dir:_files -/' \
  '(-s -config-scenario)'{-s,-config-scenario}'[Installed scenario name, scenario.toml, or a scenario directory]:path:_files' \
  '(-f -config-content)'{-f,-config-content}'[Content directory, or a single content file]:path:_files' \
  '(-k -config-keymap)'{-k,-config-keymap}'[Keymap TOML]:path:_files' \
  '-config-music[Music pattern override TOML]:path:_files' \
  '-config-sounds[Sound definition override TOML]:path:_files' \
  '-color[Colour depth; auto detects the terminal]:value:(auto 256 true)' \
  '-mute[Start muted, which is the default; -mute=false starts with sound]' \
  '(-ab -audio-backend)'{-ab,-audio-backend}'[Force an audio backend instead of detecting one]:name: ' \
  '-seed[Root RNG seed; 0 draws one and logs it]:n: ' \
  '-speed[Simulation rate 1/8 1/4 1/2 1 2 4 8; with -script also "max" for no wall pacing]:rate: ' \
  '-script[Run an authored deterministic TOML tick script]:path:_files' \
  '-watch[Present a -script run on this terminal instead of running it headlessly]' \
  '-replay[Replay a recorded journal instead of playing]:path:_files' \
  '-check[Validate the resolved scenario, keymap, audio and content, then exit]' \
  '-schema[Print the FSM schema as JSON, then exit]' \
  '(-l -log)'{-l,-log}'[Enable logging; DIR overrides $XDG_STATE_HOME/vif/log]' \
  '(-lv -log-level)'{-lv,-log-level}'[trace, debug, info, warn or error; implies -l]:level: ' \
  '(-ls -log-scope)'{-ls,-log-scope}'[Which subsystems log; see Scopes below; implies -l]:spec: ' \
  '(-lt -log-stat)'{-lt,-log-stat}'[Status snapshot period in game ticks, 0 disables; implies -l]:ticks: ' \
  '(-lr -log-recorder)'{-lr,-log-recorder}'[Flight recorder depth in game ticks, 0 disables; implies -l]:ticks: ' \
  '-log-session-id[Attach a session ID to every application log record; implies -l]:id: ' \
  '-log-stdout[Write the log to stdout as JSON instead of to a file; implies -l]' \
  '(-j -journal)'{-j,-journal}'[Record a replay journal; DIR overrides $XDG_STATE_HOME/vif/journal]' \
  '(-mw -music-wav)'{-mw,-music-wav}'[Record the music alone, as it plays, to a WAV file; DIR overrides $XDG_STATE_HOME/vif/music]' \
  '-dev[Capture runtime stderr to a file; on by default for -race builds]' \
  '(-h -help)'{-h,-help}'[Print this and exit]' \
  '-version[Print the module version and commit a package should report]'
