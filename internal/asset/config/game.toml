[regions.main]
file = "main.toml"
enabled_systems = ["glyph"]

[regions.quasar]
file = "quasar.toml"
enabled_systems = ["glyph"]

[regions.storm]
file = "storm.toml"
enabled_systems = ["glyph"]

[regions.monitor]
initial = "MonitorBoot"
file = "monitor.toml"
background = true

[regions.placeholder]
file = "placeholder.toml"

