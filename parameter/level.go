package parameter

// Level transition defaults
const (
	// Maze cell dimensions (grid cells per maze cell)
	MazeCellWidth  = 10
	MazeCellHeight = 5

	// Default maze parameters
	MazeBraiding = 0.3 // Some loops, not too many dead ends

	// Maze phase map size
	QuasarMapWidth  = 300
	QuasarMapHeight = 200
)