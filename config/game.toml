# Root FSM configuration
# External state files are merged before parsing

[regions]
main = { initial = "SpawnGold", file = "regions/main.toml" }
# Quasar region spawned dynamically via SpawnRegion action; states loaded via file
quasar = { file = "regions/quasar.toml" }